# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Purpose

Personal vacation planning project for a family of 4 (2 adults + 2 teens, ages 17 and 15) for summer 2026. The **current itinerary is Bavaria + Salzburg + Vienna, July 11–22, 2026** (`itinerary.md`). Other destination options (Greece, St. Lucia) remain in the research files for reference.

## Key Files

- `itinerary.md` — **canonical working itinerary** (day-by-day, updated as details are confirmed; contains a `<!-- COSTS_START -->…<!-- COSTS_END -->` block auto-generated by `build_costs.py`)
- `itinerary.html` — styled HTML version of the itinerary (same sentinel markers for cost injection)
- `munich_activities.md` — longlist of Munich city activities for family discussion (not all will be done)
- `munich_day_trips.md` — longlist of day-trip options from Munich
- `bavaria option 2.md` — original planning framework: day-by-day options, lodging recommendations, cost estimates
- `bavaria_itinerary_locked.md` — earlier version of the itinerary; superseded by `itinerary.md`
- `bavaria2_brochure.html` — styled HTML brochure for the trip (PDF export: `bavaria2_brochure.pdf`)
- `build_vacation_spreadsheet.py` — generates `vacation_research_2026.xlsx` with multi-tab cost comparison
- `brochure_images/` — photos used in brochures (day-by-day filenames like `day4_neuschwanstein.jpg`)
- `archive/` — all non-Bavaria files (Greece, St. Lucia research, Option 1 brochures, working screenshots)

## Bookings File

- `bookings.md` — **single source of truth for all confirmed reservations**: confirmation numbers, ticket numbers, contact info, cancellation policies, meeting logistics, and a "still to book" checklist. Add entries here as reservations are made; keep planning notes and options-under-consideration in `itinerary.md`.

## Planning Research Files (to be created)

As trip details are researched and booked, maintain these focused MD files:

- `flights.md` — Open-jaw: IAD → MUC outbound (Jul 11), VIE → IAD return (Jul 22); airlines, booking status
- `accommodation.md` — Airbnb/hotel options per city (Munich, Berchtesgaden, Salzburg) with links and status
- `trains.md` — Bayern-Ticket logistics, day-trip connections, Railjet booking
- `activities.md` — All bookable items (Eagle's Nest, Mike's Bike Tours, St. Peter Stiftskeller, etc.) with links, prices, booking priority/status

## Scripts

**Cost estimate** (primary — run whenever costs change):
```bash
python build_costs.py
```
Reads the `COSTS` list in `build_costs.py`, writes `trip_costs.xlsx`, and injects a formatted cost table into both `itinerary.md` and `itinerary.html` between `<!-- COSTS_START -->` / `<!-- COSTS_END -->` sentinels. Edit costs only in `build_costs.py`; never edit the sentinel block directly. Requires `openpyxl`.

**Multi-destination spreadsheet** (legacy):
```bash
python build_vacation_spreadsheet.py
```
Generates `vacation_research_2026.xlsx` with multi-tab cost comparisons across destination options. Cell cross-references use named sheets (e.g., `=Bavaria_Costs!F28`). Requires `openpyxl`.

## Brochure Workflow

Brochures are self-contained HTML files with inline CSS and `<img>` tags referencing `brochure_images/`. To export to PDF, open in a browser and print to PDF (or use Playwright). Screenshots of the rendered output are saved as `*_map_check.png` and `*_brochure.pdf` at the root.

## Conventions

- `*.BACKUP.md` files are gitignored; `*.BACKUP2.md` files are not — treat the non-ignored backups as reference snapshots, not the authoritative version.
- Loose JPEG files at the root (prefixed `preview_`, `check_`, or named by location) are working screenshots used during brochure development, not source assets.
- `_tmp_train_imgs.json` is a large scratch file from image search sessions — ignore it.
